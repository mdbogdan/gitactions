name: Main Workflow
on: push
jobs:
  Build:
    runs-on: ubuntu-latest
    container: gradle:6-jdk11
    steps:
      - name: Clone Down Repository
        uses: actions/checkout@v4
      - run: ci/build-app.sh
  Test:
    
